query GetCommentsOfBattle($battleId: Int!) {
  getCommentsOfBattle(battleId: $battleId) {
    id
    body
    likeDislikeCount
    userLikeDislike
    user {
      id
      email
      username
    }
  }
}
